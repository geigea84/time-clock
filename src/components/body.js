import '../App.css';
import TimeGrid from '../pages/timeGrid';

function Body() {
    return (
        <div id="body-container">
            <TimeGrid></TimeGrid>
        </div>
    );
}

export default Body;
